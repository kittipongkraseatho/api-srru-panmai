{
  "routes": [
    {
      "method": "GET",
      "path": "/plants",
      "handler": "plants.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/plants/count",
      "handler": "plants.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/plants/:id",
      "handler": "plants.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/plants",
      "handler": "plants.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/plants/:id",
      "handler": "plants.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/plants/:id",
      "handler": "plants.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
